// src/app/dept/events/[eventId]/registrants/[studentId]/page.tsx
// Re-uses advisor student application detail page — same logic, same UI
export { default } from '@/app/advisor/events/[eventId]/registrants/[studentId]/page';
